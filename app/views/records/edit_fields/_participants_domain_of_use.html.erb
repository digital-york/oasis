<%= f.input :participants_domain_of_use, as: :check_boxes,
    collection: GenericLocalAuthorityService.select_all_options('domains_of_use'),
    value_method: lambda {|v| v.last.to_s },
    input_html: { multiple: true } %>
