<%
  years = {}
  years['in press'] = 'in press'
  (1920..Date.today.year+5).reverse_each do |year|
    years[year] = year
  end
%>

<%= f.input :publication_date, as: :select,
    collection: years,
    include_blank: true,

    input_html: { class: 'form-control' }
%>