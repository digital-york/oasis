<% provide :page_title, @presenter.page_title %>

<%= render 'shared/citations' %>


<% if @presenter.solr_document.title_of_summary %>
  <%= render 'show_summary' %>
<% else %>
  <%= render 'show_downloader' %>
<% end %>
